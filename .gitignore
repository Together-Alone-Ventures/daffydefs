# Rust build artifacts
/target/

# WASM output directory (built by scripts/build.sh)
/wasm_out/

# Embedded WASM in factory source (copied by build.sh)
src/profile_factory/profile_canister_embedded.wasm

# dfx local state
.dfx/

# Frontend
src/frontend/node_modules/
src/frontend/dist/

# Generated declarations
src/frontend/src/declarations/

# Environment file (generated by dfx)
.env

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
MKTd02/
after_lib.rs
before_factory.rs
before_lib.rs
mktd02_integration_diff.txt

# Gate 2 rollback artifacts — large pre-retrofit wasms stay local; provenance in SHA256SUMS
rollback_artifacts/*.wasm

# Gate 2 W5 ceremony handoff — operator-held evidence, referenced by path from RELEASES;
# not committed (durable copies land in CVDR-Verify via W6 with pinned provenance)
ceremony_handoff_w5_remediated/

# Capsule: snapshot archives are packaging hygiene, never provenance anchors
releases/CANDIDATE/*.tar.gz
releases/CANDIDATE/capsule-src.tar
tools/cvdr-verify/target/
releases/CANDIDATE/container-output-*/
