# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "zombie-core"
version = "0.4.0"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Shared protocol types for the Zombie suite - cryptographic deletion receipts, hashing primitives, and serialisation for GDPR Right to Erasure on ICP"
readme = "README.md"
license = "Apache-2.0"
repository = "https://github.com/Together-Alone-Ventures/zombie-core"

[features]
local-replica = []

[lib]
name = "zombie_core"
path = "src/lib.rs"

[dependencies]
candid = "0.10"
ciborium = "0.2"
hex = "0.4"
sha2 = "0.10"

[dependencies.serde]
version = "1"
features = ["derive"]

[dev-dependencies]
serde_json = "1"
