[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]
candid = "0.10"
serde = { version = "1", features = ["derive"] }
ic-stable-structures = "0.6"
ic-cdk = "0.16"
